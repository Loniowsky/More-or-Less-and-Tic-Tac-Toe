language: python
python:
    - "3.6"
install:
   - pip install python-coveralls
   - pip install coverage
   - pip install nose
script:
    - nosetests --with-coverage --cover-package=tests,More_or_less, tictactoe
after_success:
    - coveralls
